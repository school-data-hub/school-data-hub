import 'package:flutter/material.dart';

class AppPaddings {
  static const EdgeInsets pupilProfileCardPadding = EdgeInsets.symmetric(
    horizontal: 5,
    vertical: 5,
  );

  static const filterChipPadding =
      EdgeInsets.symmetric(horizontal: 0, vertical: 0);

  static const filterChipLabelPadding = EdgeInsets.only(right: 10);

  static const filterChipLabelPaddingOff = EdgeInsets.only(right: 10);

  static const double bottomNavBarButtonGap = 30;
}
